/**
 * practice/types/listening/listening.js
 * Listening type alias delegating to vocabulary engine logic.
 */
(function() {
    'use strict';
    if (window.ensureDataLoaded && window.beginSession) {
        // Shared practice engine listening handler available
    }
})();
