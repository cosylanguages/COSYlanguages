/**
 * practice/types/grammar/grammar.js
 * Grammar type alias delegating to vocabulary engine logic.
 */
(function() {
    'use strict';
    if (window.ensureDataLoaded && window.beginSession) {
        // Shared practice engine grammar handler available
    }
})();
