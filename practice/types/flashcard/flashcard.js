/**
 * practice/types/flashcard/flashcard.js
 * Flashcard type alias delegating to vocabulary engine logic.
 */
(function() {
    'use strict';
    if (window.ensureDataLoaded && window.beginSession) {
        // Shared practice engine vocabulary handler available
    }
})();
