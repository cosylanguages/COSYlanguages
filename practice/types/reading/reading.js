/**
 * practice/types/reading/reading.js
 * Reading type alias delegating to vocabulary engine logic.
 */
(function() {
    'use strict';
    if (window.ensureDataLoaded && window.beginSession) {
        // Shared practice engine reading handler available
    }
})();
